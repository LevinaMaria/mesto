import { Popup } from "./Popup";

export class PopupWithImage extends Popup {
    constructor (popupConfig, config) {
        super (popupConfig, config);
    }
    open (name, link) {
        const imageView = this._
    }
}